"Main interface for {{ module.service_name.boto3_name }} type defs"
from __future__ import annotations

{% with import_record_groups=module.get_type_defs_required_import_record_groups() -%}
    {% include "common/import_groups.py.jinja2" with context -%}
{% endwith -%}

{{ "\n\n" -}}

__all__ = (
{% for typed_dict in module.typed_dicts -%}
    {{ '"' -}}
    {{ typed_dict.name -}}
    {{ '"' -}}
    {{ ",\n" if not loop.last or loop.first else "\n" }}
{% endfor -%}
)

{{ "\n\n" -}}

{% for typed_dict in module.typed_dicts -%}
    {% include "common/typed_dict.py.jinja2" with context -%}
    {{ "\n" -}}
{% endfor -%}
