try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_mediastore_data.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[mediastore-data] to use mediastore-data Paginator annotations"
    )
