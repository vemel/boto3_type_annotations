try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_mq.paginator import *
except ImportError:
    raise ImportError("Install boto3-stubs[mq] to use mq Paginator annotations")
