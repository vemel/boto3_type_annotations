try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_snowball.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[snowball] to use snowball Paginator annotations"
    )
