try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_personalize.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[personalize] to use personalize Paginator annotations"
    )
