try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_glacier.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[glacier] to use glacier Paginator annotations"
    )
