try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_ecs.paginator import *
except ImportError:
    raise ImportError("Install boto3-stubs[ecs] to use ecs Paginator annotations")
