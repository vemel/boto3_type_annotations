try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_marketplace_entitlement.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[marketplace-entitlement] to use marketplace-entitlement Paginator annotations"
    )
