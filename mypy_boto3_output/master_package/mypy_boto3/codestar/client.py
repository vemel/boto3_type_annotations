try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_codestar.client import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[codestar] to use codestar Client annotations"
    )
