try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_codestar.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[codestar] to use codestar Paginator annotations"
    )
