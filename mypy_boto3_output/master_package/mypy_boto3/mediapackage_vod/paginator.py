try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_mediapackage_vod.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[mediapackage-vod] to use mediapackage-vod Paginator annotations"
    )
