try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_translate.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[translate] to use translate Paginator annotations"
    )
