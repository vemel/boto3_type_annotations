try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_workmail.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[workmail] to use workmail Paginator annotations"
    )
