try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_dlm.paginator import *
except ImportError:
    raise ImportError("Install boto3-stubs[dlm] to use dlm Paginator annotations")
