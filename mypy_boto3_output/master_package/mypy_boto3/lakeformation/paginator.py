try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_lakeformation.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[lakeformation] to use lakeformation Paginator annotations"
    )
