try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_lakeformation import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[lakeformation] to use lakeformation annotations"
    )
