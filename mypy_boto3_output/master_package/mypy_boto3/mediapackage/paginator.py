try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_mediapackage.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[mediapackage] to use mediapackage Paginator annotations"
    )
