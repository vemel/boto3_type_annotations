try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_resource_groups.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[resource-groups] to use resource-groups Paginator annotations"
    )
