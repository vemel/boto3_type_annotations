try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_resource_groups import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[resource-groups] to use resource-groups annotations"
    )
