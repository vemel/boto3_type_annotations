try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_cloudwatch.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[cloudwatch] to use cloudwatch Paginator annotations"
    )
