try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_cloudwatch.client import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[cloudwatch] to use cloudwatch Client annotations"
    )
