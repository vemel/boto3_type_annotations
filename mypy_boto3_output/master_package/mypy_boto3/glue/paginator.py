try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_glue.paginator import *
except ImportError:
    raise ImportError("Install boto3-stubs[glue] to use glue Paginator annotations")
