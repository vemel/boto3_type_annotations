try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_cloudtrail.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[cloudtrail] to use cloudtrail Paginator annotations"
    )
