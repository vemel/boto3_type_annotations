try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_transfer.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[transfer] to use transfer Paginator annotations"
    )
