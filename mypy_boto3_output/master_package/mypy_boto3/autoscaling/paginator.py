try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_autoscaling.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[autoscaling] to use autoscaling Paginator annotations"
    )
