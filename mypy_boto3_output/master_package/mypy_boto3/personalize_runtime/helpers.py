try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_personalize_runtime.helpers import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[personalize-runtime] to use personalize-runtime annotations"
    )
