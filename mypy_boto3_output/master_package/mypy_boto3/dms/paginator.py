try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_dms.paginator import *
except ImportError:
    raise ImportError("Install boto3-stubs[dms] to use dms Paginator annotations")
