try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_budgets.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[budgets] to use budgets Paginator annotations"
    )
