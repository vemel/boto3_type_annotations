try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_worklink.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[worklink] to use worklink Paginator annotations"
    )
