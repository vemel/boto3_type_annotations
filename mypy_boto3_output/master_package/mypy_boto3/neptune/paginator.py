try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_neptune.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[neptune] to use neptune Paginator annotations"
    )
