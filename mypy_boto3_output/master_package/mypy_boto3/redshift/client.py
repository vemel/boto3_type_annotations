try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_redshift.client import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[redshift] to use redshift Client annotations"
    )
