try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_redshift.helpers import *
except ImportError:
    raise ImportError("Install boto3-stubs[redshift] to use redshift annotations")
