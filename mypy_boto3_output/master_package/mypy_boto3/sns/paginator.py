try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_sns.paginator import *
except ImportError:
    raise ImportError("Install boto3-stubs[sns] to use sns Paginator annotations")
