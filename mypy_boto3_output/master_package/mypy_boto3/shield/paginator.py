try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_shield.paginator import *
except ImportError:
    raise ImportError("Install boto3-stubs[shield] to use shield Paginator annotations")
