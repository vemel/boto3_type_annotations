try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_workspaces.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[workspaces] to use workspaces Paginator annotations"
    )
