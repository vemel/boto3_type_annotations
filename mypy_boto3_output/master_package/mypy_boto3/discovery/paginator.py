try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_discovery.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[discovery] to use discovery Paginator annotations"
    )
