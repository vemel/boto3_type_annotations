try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_organizations.type_defs import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[organizations] to use organizations annotations"
    )
