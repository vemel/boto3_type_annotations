try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_codepipeline.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[codepipeline] to use codepipeline Paginator annotations"
    )
