try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_pricing.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[pricing] to use pricing Paginator annotations"
    )
