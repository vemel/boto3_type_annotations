try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_inspector.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[inspector] to use inspector Paginator annotations"
    )
