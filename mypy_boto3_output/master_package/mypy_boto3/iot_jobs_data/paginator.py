try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_iot_jobs_data.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[iot-jobs-data] to use iot-jobs-data Paginator annotations"
    )
