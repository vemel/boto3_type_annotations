try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_ssm.paginator import *
except ImportError:
    raise ImportError("Install boto3-stubs[ssm] to use ssm Paginator annotations")
