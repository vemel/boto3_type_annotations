try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_textract.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[textract] to use textract Paginator annotations"
    )
