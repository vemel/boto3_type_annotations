try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_service_quotas.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[service-quotas] to use service-quotas Paginator annotations"
    )
