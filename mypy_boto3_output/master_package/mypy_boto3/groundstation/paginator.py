try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_groundstation.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[groundstation] to use groundstation Paginator annotations"
    )
