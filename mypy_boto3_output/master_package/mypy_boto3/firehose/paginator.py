try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_firehose.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[firehose] to use firehose Paginator annotations"
    )
