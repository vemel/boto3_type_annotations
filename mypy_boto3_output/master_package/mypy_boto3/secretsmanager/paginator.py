try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_secretsmanager.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[secretsmanager] to use secretsmanager Paginator annotations"
    )
