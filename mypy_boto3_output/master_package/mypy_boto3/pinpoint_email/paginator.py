try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_pinpoint_email.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[pinpoint-email] to use pinpoint-email Paginator annotations"
    )
