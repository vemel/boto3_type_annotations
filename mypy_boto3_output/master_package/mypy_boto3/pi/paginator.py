try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_pi.paginator import *
except ImportError:
    raise ImportError("Install boto3-stubs[pi] to use pi Paginator annotations")
