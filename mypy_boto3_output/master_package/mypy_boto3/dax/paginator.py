try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_dax.paginator import *
except ImportError:
    raise ImportError("Install boto3-stubs[dax] to use dax Paginator annotations")
