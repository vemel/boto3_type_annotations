try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_directconnect.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[directconnect] to use directconnect Paginator annotations"
    )
