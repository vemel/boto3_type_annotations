try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_directconnect.helpers import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[directconnect] to use directconnect annotations"
    )
