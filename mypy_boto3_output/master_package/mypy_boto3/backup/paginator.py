try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_backup.paginator import *
except ImportError:
    raise ImportError("Install boto3-stubs[backup] to use backup Paginator annotations")
