try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_codecommit.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[codecommit] to use codecommit Paginator annotations"
    )
