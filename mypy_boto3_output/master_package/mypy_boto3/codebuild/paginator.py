try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_codebuild.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[codebuild] to use codebuild Paginator annotations"
    )
