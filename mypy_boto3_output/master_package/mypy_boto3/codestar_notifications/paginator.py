try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_codestar_notifications.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[codestar-notifications] to use codestar-notifications Paginator annotations"
    )
