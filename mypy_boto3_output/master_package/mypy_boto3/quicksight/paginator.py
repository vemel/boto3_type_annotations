try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_quicksight.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[quicksight] to use quicksight Paginator annotations"
    )
