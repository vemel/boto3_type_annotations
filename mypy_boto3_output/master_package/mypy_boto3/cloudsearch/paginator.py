try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_cloudsearch.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[cloudsearch] to use cloudsearch Paginator annotations"
    )
