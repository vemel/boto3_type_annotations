try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_iam.paginator import *
except ImportError:
    raise ImportError("Install boto3-stubs[iam] to use iam Paginator annotations")
