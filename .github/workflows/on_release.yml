name: Bump version and dockerize

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Extract variables from env
        id: vars
        run: |
          echo "GitHub ref ${GITHUB_REF}"
          VERSION=`echo "${GITHUB_REF}" | cut -f 3 -d "/" | cut -f 1 -d "-"`
          echo "Got version ${VERSION}"
          echo "##[set-output name=version;]$(echo ${VERSION})"
      - name: Set up Python
        uses: actions/setup-python@v1
        with:
          python-version: "3.8"
      - name: Install builder dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r builder/requirements.txt
      - name: Install builder
        run: |
          cd builder
          python setup.py install
          cd -
      - name: Install test dependencies
        run: |
          python -m pip install pylint mypy pytest
      - name: Lint
        run: |
          pylint builder/mypy_boto3_builder
          mypy builder/mypy_boto3_builder
      - name: Unit tests
        run: |
          pytest
      - name: Bump version
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ steps.vars.outputs.version }}
        run: |
          scripts/bump_version.sh
      - name: Dockerize
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ steps.vars.outputs.version }}
        run: |
          scripts/dockerize.sh
      - name: Install dependencies for publishing
        run: |
          python -m pip install setuptools wheel twine
      - name: Publish to PyPI
        env:
          TWINE_USERNAME: ${{ secrets.PYPI_USERNAME }}
          TWINE_PASSWORD: ${{ secrets.PYPI_PASSWORD }}
        run: |
          cd builder
          rm -rf build *.egg-info dist/*
          python setup.py build sdist bdist_wheel
          twine upload dist/*
          rm -rf build *.egg-info dist/*
      - name: Get version
        id: vars
        run: |
          LAST_VERSION=`pip search boto3-stubs | grep -Po '^boto3\-stubs \S+' | grep -Po '\d+\.\d+\.\d+(\.\d+)?'`
          VERSION=`echo "${LAST_VERSION} | awk 'BEGIN{FS=OFS="."}{$4=$4+1; print$0;}"`
          SKIP=""
          echo Last version: ${LAST_VERSION}
          echo Build version: ${VERSION}
          echo "##[set-output name=version;]$(echo ${VERSION})"
      - name: Build packages
        env:
          VERSION: ${{ steps.vars.outputs.version }}
        run: |
          rm -rf mypy_boto3_output/*
          scripts/build.sh --panic -d -b $VERSION
      - name: Install integration test dependencies
        run: |
          python -m pip install mypy
      - name: Integration tests
        run: |
          scripts/integration.sh
      - name: Install dependencies for publishing
        run: |
          python -m pip install setuptools wheel twine
      - name: Publish to PyPI
        env:
          TWINE_USERNAME: ${{ secrets.PYPI_USERNAME }}
          TWINE_PASSWORD: ${{ secrets.PYPI_PASSWORD }}
        run: |
          scripts/release.sh
